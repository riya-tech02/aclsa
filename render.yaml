services:
  - type: web
    name: aclsa-api
    runtime: python
    buildCommand: pip install -r services/api_gateway/requirements.txt
    startCommand: cd services/api_gateway && uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
