services:
  - type: web
    name: aclsa-agent-api
    env: docker
    plan: free
    dockerfilePath: services/api_gateway/Dockerfile
    dockerContext: .
    autoDeploy: true
    envVars:
      - key: PYTHONPATH
        value: /app
